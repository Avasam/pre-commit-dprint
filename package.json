{
    "name": "placeholder_package",
    "description": "Note: double curly-braces because python .format",
    "version": "0.0.0",
    "scripts": {
      "postinstall": "echo %PWD% && cd %PWD% && dprint check --config https://raw.githubusercontent.com/BesLogic/shared-configs/main/dprint.json"
    }
}
