{
    "name": "placeholder_package",
    "description": "Note: double curly-braces because python .format",
    "version": "0.0.0",
    "dependencies": {
        "cross-env": "latest"
    },
    "scripts": {
      "postinstall": "cross-env-shell \"echo $PWD && cd $PWD && dprint check\""
    }
}
